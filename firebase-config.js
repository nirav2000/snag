// Optional deployment-wide Firebase config. Paste the standard Firebase web config below.
// Users can also enter the same config in Settings; it is stored in this browser only.
window.SNAG_FIREBASE_CONFIG = null;
